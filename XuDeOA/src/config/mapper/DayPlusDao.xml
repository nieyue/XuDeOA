<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.nieyue.dao.DayPlusDao">
<!-- 添加日加人数 -->
<insert id="addDayPlus" parameterType="DayPlus" flushCache="true"  >  
   INSERT IGNORE INTO
   		  day_plus_tb 
   		 (
   		 update_date,
   		 total_number,
   		 retain_number
   		 )
   VALUES
    	 (
    	 #{updateDate},
    	 #{totalNumber},
    	 #{retainNumber}
    	 )  
</insert>  
<!-- 更新日加人数 -->
<update id="updateDayPlus" parameterType="DayPlus" >  
    UPDATE 
    	day_plus_tb 
    SET 
    	update_date=#{updateDate},
    	total_number=#{totalNumber},
    	retain_number=#{retain_number}
    WHERE
    	 day_plus_id=#{dayPlusId} 
</update> 
<!-- 删除日加人数-->
<delete id="delDayPlus"  parameterType="Integer" >  
    DELETE FROM
    	 day_plus_tb 
    	 WHERE day_plus_id=#{dayPlusId}
</delete>  
<!-- 加载日加人数-->
<select id="loadDayPlus" parameterType="Integer" resultType="DayPlus" >
SELECT 
		dayPlus_id,
	 	update_date,
    	total_number,
    	retain_number
FROM day_plus_tb 
WHERE day_plus_id=#{dayPlusId}
</select>

<!-- 分页查询日加人数-->
<select id="browsePagingDayPlus" resultType="DayPlus">  
    SELECT
	    dayPlus_id,
	 	update_date,
    	total_number,
    	retain_number
	FROM day_plus_tb
	ORDER BY
		${orderName}  ${orderWay}
	LIMIT
		#{pageNum},#{pageSize}
</select> 

<!-- 统计记录条数 -->
<select id="countAll"  resultType="int">  
	SELECT
	 	count(day_plus_id)
	FROM day_plus_tb
</select>   
   
 
</mapper>